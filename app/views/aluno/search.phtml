<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(array("aluno/index", "Voltar")); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(array("aluno/new", "Criar ")); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h1>Relatorios de Alunos </h1>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Código de matricula </th>
            <th>Descrição do aluno</th>
            <th>Data do cadastro do Aluno</th>
            <th>Situação do aluno</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $aluno): ?>
            <tr>
                <td><?php echo $aluno->codmat ?></td>
            <td><?php echo $aluno->desalu ?></td>
            <td><?php echo $aluno->dascadalu ?></td>
            <td><?php echo $aluno->sitalu ?></td>

                <td><?php echo $this->tag->linkTo(array("aluno/edit/" . $aluno->codmat, "Editar")); ?></td>
                <td><?php echo $this->tag->linkTo(array("aluno/delete/" . $aluno->codmat, "Deletar")); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo("aluno/search", "Primeiro") ?></li>
                <li><?php echo $this->tag->linkTo("aluno/search?page=" . $page->before, "Anterior") ?></li>
                <li><?php echo $this->tag->linkTo("aluno/search?page=" . $page->next, "Proximo") ?></li>
                <li><?php echo $this->tag->linkTo("aluno/search?page=" . $page->last, "Final") ?></li>
            </ul>
        </nav>
    </div>
</div>
